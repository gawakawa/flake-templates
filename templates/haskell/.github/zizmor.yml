rules:
  # haskellNix pins an older zizmor (1.23.1) that flags this; newer zizmor doesn't.
  secrets-outside-env:
    ignore:
      - ci.yml
  unpinned-uses:
    config:
      policies:
        gawakawa/*: any
